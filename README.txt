Coordinador Delivery Gusto:

- Recibe imagen+texto
- Normaliza texto hacia numero telefónico (tolerancia a errores)
- Descarga imagen y numero telefónico
- Reenvia imagen hacia numero telefónico

Infraestructura:

- Meta Cloud API - Render - WhatsApp

Link de entrega al cliente para seguimiento:
https://wa.me/56953303915?text=SEGUIMIENTO

Para commits (en carpeta):

git init
git add .
git status
git config --global user.name "Nomnbre Apellido"
git config --global user.email "email@gmail.com"
git commit -m "Initial WhatsApp coordinator"
git branch -M main
git remote add origin https://github.com/gustoagent-ai/coordinador_delivery.git
git push -u origin main

